image:
  repository: 548112056811.dkr.ecr.us-west-1.amazonaws.com/mern-frontend
  tag: latest

replicaCount: 1

version: blue  # ✅ Added version key to support blue/green Helm releases

service:
  type: ClusterIP
  port: 80

env:
  API_URL: "http://backend-service:5000"  # ✅ Corrected to match backend service name & port

resources: {}
